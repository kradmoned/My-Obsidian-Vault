# IP Address

- Also known as logical address
- IP stands for "Internet Protocol"
- It is represented in Decimal
- Example include IPV6 and IPv4

## Octets

- IP address is Represented in format _W.X.Y.Z_ -> 172.30.10.11
  - These are known as the four octets
    - Each Octet can have a value of 0 - 255
    - Each Octet has 8 bits

## IPV4 and IPV6

Ipv4 is 32 bits while ipv6 is 128 bits

# Mac Address

- Also known as physical address, hardware address
- Represented in hex
- is 48 bits
- What is the company identifier and serial number in mac address

---

# Network

There are several logical layers developed for communication between 2 end points. There are in total 7 layers abbreviated as **APSTNDP** .

**OSI MODEL**(Basic Explanation)

| **Numbers** | **Layers**         | **Example**                                                  |
| ----------- | ------------------ | ------------------------------------------------------------ |
| 7           | Application Layers | Browser                                                      |
| 6           | Presentation Layer | Transform the data into something such as encode it etc      |
| 5           | Session Layer      | Create a session with provider such as google                |
| 4           | Transport LAyer    | Send data through netword                                    |
| 3           | Network Layer      | Fix the errors etc, _IP addressing is part of network layer_ |
| 2           | Data Link Layer    | _Mac address is part of DATA link layer_                     |
| 1           | Physical Layer     | The physical cable                                           |

---

01/27/2026

Bits
Bytes
Kilobytes
Megabytes
Gigabytes
TeraBytes

The thing That is closest to the regiseter is Register, Cache(L0,L1,L2), Ram.
The thing that is the most expensive is the registers and the cheapes is the Storage\*Hardsis)

# LAN vs > WAN

Lan stands for local area network -> Pieas uses PERN? What is that

# Latency/Ping

The delay that is between the source and destination
The lesser the latency the better is your network.

# Corruption

It is the alteration of data , it could have been done purposefuly or by mistake(Distortion)

- Distortion Occurs when the physical link between data causes some accidental change in data.
- Purposefully is done by malicious actors
